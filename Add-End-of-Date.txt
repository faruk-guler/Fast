$Date = Get-Date "2026-04-31".
$OU   = "OU=TestUsers,DC=guler,DC=com"

Get-ADUser -Filter * -SearchBase $OU | ForEach-Object {
    Set-ADUser -Identity $_.SamAccountName -AccountExpirationDate $Date
    Write-Host "Guncellendi: $($_.SamAccountName)"
}
